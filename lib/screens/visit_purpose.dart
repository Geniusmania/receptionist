import 'package:flutter/material.dart';

class VisitPurpose extends StatelessWidget {
  const VisitPurpose({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}